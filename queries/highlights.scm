(comment) @comment @spell

(definition) @keyword

(identifier) @variable

(label_name) @label

(token) @string

(generic_token) @type

(literal_token) @string.special

[
  "="
  "|"
] @operator

[
  "*"
  "?"
] @operator.repeat

":" @punctuation.delimiter

[
  "("
  ")"
] @punctuation.bracket
